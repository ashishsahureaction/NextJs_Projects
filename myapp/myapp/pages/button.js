'use client';

import React from 'react'

const Button = () => {
    
  return (
    <div>button ko check karo

        <button onClick={()=>console.log("check event working")}>Add to check button</button>
    </div>
  )
}

export default Button